/*
import { SuccessResponse } from "src/utils/interfaces/ApiResponse";

export interface LoginDto {
  correo: string;
  contraseña: string;
}

export interface LoginUser {
  id: number;
  nombre: string;
  correo: string;
}

export type LoginResponse = SuccessResponse<{ token: string; usuario: LoginUser }>;

*/